# 我的在线简历

嵌入式软件工程师简历，通过 Gitee Pages 在线托管。

## 在线访问

开启 Gitee Pages 后访问：`https://tangjialibiubiubiu.gitee.io/<仓库名>`

## 如何修改

1. 在 Gitee 仓库中直接在线编辑 `index.html`，或本地修改后 `git push`；
2. 进入仓库「服务 → Gitee Pages」，点击「更新」使改动生效。

## 本地导出 PDF

用浏览器打开 `index.html`，按 `Ctrl+P` → 目标选「另存为 PDF」→ 勾选「背景图形」→ 保存。
